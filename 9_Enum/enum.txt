

                            Online C Compiler.
                Code, Compile, Run and Debug C program online.
Write your code in this editor and press Run button to compile and execute it.



#include stdio.h
typedef enum Volume Volume;
enum Volume
{
    MUTE,
    LOW=5,
    MEDIUM,
    HIGH
};
int main()
{
    printf(Volum1=%dn,MUTE);
    printf(Volum2=%dn,LOW);
    printf(Volum2=%dn,MEDIUM);
    printf(Sizeof Enum=%ld,sizeof(Volume));
}